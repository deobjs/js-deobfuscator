{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "example",
    "website"
  ],
  "type": "module",
  "scripts": {
    "website:dev": "bun run --cwd website dev",
    "website:build": "bun run --cwd website build",
    "website:generate": "bun run --cwd website generate",
    "build": "bun run --cwd packages/deob build",
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "vitest"
  },
  "devDependencies": {
    "deob": "workspace:*",
    "oxfmt": "^0.20.0",
    "oxlint": "^1.35.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
